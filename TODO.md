# TODO

Planned features and improvements for HRVey.

## Features

- [ ] **Customizable keyboard shortcuts** — let users remap the keys used for app actions, with a settings UI and sensible defaults.
- [ ] **Clear currently loaded ECG completely** — a single action to fully reset the loaded ECG and any derived data/state.
- [ ] **Custom participant fields** — allow users to define their own participant metadata fields (e.g. name, group, notes) beyond the built-in ones.
- [ ] **Save data between sessions (local machine)** — persist loaded ECGs, participant info, and settings locally (e.g. localStorage / IndexedDB) so work survives a reload or restart.
